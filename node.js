export class Node {
    constructor(value) {
      this.value = value;  // Value stored in the node
      this.next = null;   // Reference to the next node in the list
      this.prev = null;   // Reference to the next node in the list
    }
  }